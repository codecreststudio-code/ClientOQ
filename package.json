{
  "name": "clientoq-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "turbo run dev --filter=@clientoq/web",
    "build": "turbo run build --filter=@clientoq/web",
    "lint": "turbo run lint --filter=@clientoq/web",
    "test": "turbo run test --filter=@clientoq/web"
  },
  "devDependencies": {
    "prisma": "^5.10.0",
    "turbo": "^2.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@prisma/client",
      "@prisma/engines",
      "prisma",
      "bcrypt"
    ]
  },
  "packageManager": "pnpm@10.33.2"
}
